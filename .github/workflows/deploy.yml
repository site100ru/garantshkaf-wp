name: Deploy garantshkaf

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Connect to server
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.GARANTSHKAF_HOST }}
          username: ${{ secrets.GARANTSHKAF_USER }}
          key: ${{ secrets.GARANTSHKAF_KEY }}
          script: |
            #Переключаемся в нужную папку
            cd ~/garantshkaf.ru/public_html/wp-content/themes/site100garantshkaf
            
            #Клинируем репозиторий в папку (только для первой выгрузки!!!)
            git clone https://github.com/site100ru/garantshkaf.git .
            
            #Клинируем репозиторий в папку
            #git pull
            
            #Если кто то изменил файлы на сервере минуя GitHub, перезаписываем версию на сервере из GitHub
            #git reset --hard HEAD
            #git pull origin main
